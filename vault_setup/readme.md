```bash
Repository to set up a Vault dev server on your own:

This repository includes a Terraform script along with a template file that contains a bootstrap script to install and configure Vault.
Finally, the Terraform output section provides the public and private endpoints of the Vault server.

```
